'use strict';

const { Goal } = require('./models');
const { router } = require('./router');

module.exports = { Goal, router };

//Old Code
/*const { GoalPost, Goal } = require('./models');
const { router } = require('./router');

module.exports = { GoalPost, Goal, router };*/

